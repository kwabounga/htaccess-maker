import { CodePreviewSlicerPipe } from './code-preview-slicer.pipe';

describe('CodePreviewSlicerPipe', () => {
  it('create an instance', () => {
    const pipe = new CodePreviewSlicerPipe();
    expect(pipe).toBeTruthy();
  });
});
